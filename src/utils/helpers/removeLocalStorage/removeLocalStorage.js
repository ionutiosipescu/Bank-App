// import persistStore from "redux-persist/es/persistStore";

// export const removePersistedState = () => {
//   // Remove the persisted state from the local storage
//   persistStore().purge();

//   // Set the initial state as the persisted state
//   persistStore().persist({}, () => {});
// };

// export const removePersistedState = () => {
//   localStorage.removeItem("register");
// };
